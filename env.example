# URL del servicio web a monitorear
SERVICE_URL=https://ejemplo.com/api/health

# Configuración SMTP para envío de emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu-password-o-app-password

# Email remitente
EMAIL_FROM=monitoreo@tudominio.com

# Email destinatario para las alertas
ALERT_EMAIL=a@b.com

# Configuración alternativa para otros proveedores SMTP:

# Para Outlook/Hotmail:
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Para Yahoo:
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Para servidores SMTP corporativos:
# SMTP_HOST=mail.tuempresa.com
# SMTP_PORT=25
# SMTP_SECURE=false

# Nota: Si usas Gmail, necesitas habilitar "Apps menos seguras" 
# o preferiblemente usar un "App Password" específico